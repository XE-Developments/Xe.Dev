const nav=document.querySelector('nav'),menu=document.querySelector('.menu');menu.addEventListener('click',()=>nav.classList.toggle('open'));document.querySelectorAll('nav a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));document.getElementById('year').textContent=new Date().getFullYear();const pointer=document.querySelector('.pointer');addEventListener('pointermove',e=>{pointer.style.left=e.clientX+'px';pointer.style.top=e.clientY+'px'});document.querySelectorAll('a,button,.service-list article').forEach(el=>{el.addEventListener('mouseenter',()=>{pointer.style.width='34px';pointer.style.height='34px'});el.addEventListener('mouseleave',()=>{pointer.style.width='16px';pointer.style.height='16px'})});const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.animate([{opacity:0,transform:'translateY(22px)'},{opacity:1,transform:'translateY(0)'}],{duration:700,easing:'cubic-bezier(.2,.7,.2,1)',fill:'forwards'})}),{threshold:.08});document.querySelectorAll('.section-head,.service-list article,.statement>div,.approach-grid>*,.about-grid>*,.contact h2').forEach(x=>io.observe(x));
